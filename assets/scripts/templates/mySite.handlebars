<!-- New  -->
<br>
  <div class="panel panel-info" data-id="{{site.site.id}}">
    {{#if site.site.title}}
      <div class="panel-heading">{{site.site.title}}</div>
    {{else}}
      <div class="panel-heading">No Title</div>
    {{/if}}
      <div class="panel-body">
        {{site.site.description}}
      </div>
    </div>
  </div>
