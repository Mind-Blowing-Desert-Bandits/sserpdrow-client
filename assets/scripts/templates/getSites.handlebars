{{#each sites as |site|}}

 <!-- Developers, why are we using data-id and not just id? -->
<div class="col-xs-8 col-md-8 minibox">
  <h1 data-id="{{site.id}}"> <small>Title: {{site.title}}</small></h1>

</div>
{{/each}}
