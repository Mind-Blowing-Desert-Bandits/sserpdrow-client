{{#each pages as |page|}}

 <!-- Developers, why are we using data-id and not just id? -->
<div class="pages">
  <p data-id="{{page.id}}">
    <a href="#">
    {{#if page.type}}
    <small>{{page.type}} /</small>
    {{else}}
    <b>No Page</b>
    {{/if}}</a>
    <br>
  </p>

</div>
{{/each}}
